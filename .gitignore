# Donnees reelles -- ne doivent JAMAIS etre publiees dans ce depot (public).
# Le vrai fichier de planning (personnel, affectations, conges, gardes...) ne doit vivre que dans
# le depot prive planning-imagerie-data, jamais ici.
*.json
planning-imagerie.json

# Extracts ARI (absences) -- contiennent de vraies donnees, jamais publiees ici (voir import ARI
# dans app.js, 23/07/2026). Meme regle que les .json : uniquement du CODE dans ce depot.
*.xlsx

# Config locale de l'environnement de dev (Claude Code) -- pas necessaire a l'hebergement.
.claude/

# Doc interne (regles de gestion, contexte projet) -- volontairement HORS du depot public.
# Vit uniquement en local + dans le depot prive planning-imagerie-data (voir README).
CLAUDE.md
regles-gestion.md
